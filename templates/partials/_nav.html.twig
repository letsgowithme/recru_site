<nav class="navbar navbar-expand-lg navbar-dark bg-primary text-light">
	<div class="container-fluid">
		<a class="navbar-brand" href="#"></a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link fs-5 active fw-bold" href="/">Accueil
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link fs-5 fw-bold" href="{{ path('job.index') }}">Offres d'emploi</a>
				</li>
				{# {% if is_granted('ROLE_ADMIN') %}
					<li class="nav-item lg-ms-5">
						<a class="nav-link fs-5 fw-bold" href="{{ path('job.all') }}">
							Offres</a>
					</li>
				{% elseif app.user %}
					<li class="nav-item">
						<a class="nav-link fs-5 fw-bold" href="{{ path('job.index') }}">Mes offres</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link fs-5 fw-bold" href="{{ path('job.index_public') }}">Offres </a>
					</li>
				{% endif %} #}
			</ul>
			{% if not app.user %}
			<ul class="navbar-nav me-1 d-lg-flex align-items-lg-end">
				<li class="nav-item me-2 fs-5 fw-bold lg-ms-4">
					<a class="nav-link" href="/login">Connexion</a>
				</li>
				<li class="nav-item me-4 fs-5 fw-bold">
					<a class="nav-link" href="/registration">Inscription</a>
				</li>
			</ul>
			{% endif %}
		</div>
	</div>
	<div class="d-flex">
		<ul class="navbar-nav me-auto">
			{% if app.user %}
				<li class="nav-item dropdown" style="margin-right: 5rem;">
					<a class="nav-link dropdown-toggle fs-5 fw-bold" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false" style="color: white;">{{ app.user.firstname }} {{ app.user.lastname }}</a>
					<div class="dropdown-menu">
						{% if 'ROLE_ADMIN' in app.user.roles %}
							<a class="dropdown-item  fw-bold" href="/admin">Tableau de bord</a>
						{% endif %}
						<div class="dropdown-divider"></div>
						{# <a class="dropdown-item fw-bold" href="{{ path('security.logout') }}">Déconnextion</a> #}
										{% endif %}
				</li>
			</div>
		</li>
	</ul>
</div>
</div></nav> 
